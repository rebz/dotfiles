#!/bin/bash
# https://github.com/larrybotha/designer-dotfiles
# https://github.com/mathiasbynens/dotfiles/blob/master/.macos

# Hide "last login" line when starting a new terminal session
touch $HOME/.hushlogin

# macOS defaults (Finder, Dock, screenshots, power) live in osx/set-defaults.sh



#################################################################
### XCode Command Line Tools
#################################################################

echo 'install xcode command line tools'
echo '-----------------'
xcode-select --install
echo 'If a dialog appeared, finish installing Command Line Tools before continuing.'
read -p 'Press enter to continue...'



#################################################################
### GIT
#################################################################

# symlink git config + global gitignore (gitconfig sets excludesfile)
ln -s $HOME/.dotfiles/shell/.gitignore_global $HOME/.gitignore_global
ln -s $HOME/.dotfiles/shell/.gitconfig $HOME/.gitconfig


#################################################################
### ZSH
#################################################################

# install oh-my-zsh (RUNZSH/CHSH keep the installer from hijacking this script)
echo 'install oh-my-zsh'
echo '-----------------'
rm -rf $HOME/.oh-my-zsh
RUNZSH=no CHSH=no sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
git clone https://github.com/zsh-users/zsh-autosuggestions $HOME/.oh-my-zsh/custom/plugins/zsh-autosuggestions
git clone https://github.com/zsh-users/zsh-syntax-highlighting $HOME/.oh-my-zsh/custom/plugins/zsh-syntax-highlighting
git clone https://github.com/zsh-users/zsh-history-substring-search $HOME/.oh-my-zsh/custom/plugins/zsh-history-substring-search
git clone https://github.com/zsh-users/zsh-completions $HOME/.oh-my-zsh/custom/plugins/zsh-completions

# symlink zsh prefs
rm -f $HOME/.zshrc
ln -s $HOME/.dotfiles/shell/.zshrc $HOME/.zshrc



#################################################################
### Homebrew
#################################################################

echo 'install homebrew'
echo '----------------'
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# make brew usable in this shell (Apple Silicon)
eval "$(/opt/homebrew/bin/brew shellenv)"

echo 'install packages from Brewfile'
echo '----------------'
brew trust ngrok/ngrok 2>/dev/null || true
brew trust steipete/tap 2>/dev/null || true
brew trust stripe/stripe-cli 2>/dev/null || true
# --verbose streams each install's real output (download progress bars)
# instead of the silent one-line-per-package mode. mas items (Xcode etc.)
# still print nothing while downloading — watch those in the App Store app.
brew bundle --verbose --file="$HOME/.dotfiles/Brewfile"



#################################################################
### iTerm2 profile (Maple Mono NF font)
#################################################################

echo 'install iTerm2 dynamic profile'
echo '----------------'
mkdir -p "$HOME/Library/Application Support/iTerm2/DynamicProfiles"
cp "$HOME/.dotfiles/iterm2/dotfiles-profile.json" "$HOME/Library/Application Support/iTerm2/DynamicProfiles/"
# make it the default profile (guid must match iterm2/dotfiles-profile.json)
defaults write com.googlecode.iterm2 "Default Bookmark Guid" -string "d07f11e5-0000-4000-8000-000000000001"



#################################################################
### Alfred preferences
#################################################################

# point Alfred's sync at the repo snapshot — restores workflows, themes,
# and the ⌘Space hotkey (Spotlight's ⌘Space is disabled in osx/set-defaults.sh)
echo 'configure Alfred preferences sync'
echo '----------------'
# shellcheck disable=SC2088  # literal tilde is intentional: Alfred stores this pref tilde-relative
defaults write com.runningwithcrayons.Alfred-Preferences syncfolder "~/.dotfiles/alfred"
echo 'NOTE: re-enter your Alfred Powerpack license manually (not stored in repo)'



#################################################################
### Editor settings (VS Code + Cursor share vscode/)
#################################################################

echo 'link shared editor settings'
echo '----------------'
bash "$HOME/.dotfiles/scripts/link-editor-settings.sh"



#################################################################
### rclone config
#################################################################

# real config is gitignored (holds R2 credentials) — copy it from the old
# machine into ~/.dotfiles/rclone/rclone.conf alongside .env
if [ -f "$HOME/.dotfiles/rclone/rclone.conf" ]; then
    mkdir -p "$HOME/.config/rclone"
    ln -sfn "$HOME/.dotfiles/rclone/rclone.conf" "$HOME/.config/rclone/rclone.conf"
    echo 'rclone config linked'
else
    echo 'rclone: no rclone/rclone.conf found — copy it from the old machine or run `rclone config` (see rclone/rclone.conf.example)'
fi



#################################################################
### Node
#################################################################

echo 'install NVM + Node 24.19.0 LTS'
echo '---------------'
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm install 24.19.0
nvm alias default 24.19.0
git clone https://github.com/lukechilds/zsh-nvm ~/.oh-my-zsh/custom/plugins/zsh-nvm

# Grok Build (xAI) — no brew package; official npm distribution
npm install -g @xai-official/grok

# Ionic CLI for puffrate native/ iOS work
npm install -g @ionic/cli



#################################################################
### Done
#################################################################

echo '++++++++++++++++++++++++++++++'
echo '++++++++++++++++++++++++++++++'
echo 'all done'

echo '++++++++++++++++++++++++++++++'
echo 'Some optional tidbits'

echo '1. set some sensible os x defaults by running: $HOME/.dotfiles/osx/set-defaults.sh'
echo '2. make a .dotfiles-custom/shell/.(exports,aliases,functions,zshrc) for your personal commands'

echo '++++++++++++++++++++++++++++++'
echo 'Please report issues!'
